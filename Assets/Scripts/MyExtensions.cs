using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public static class MyExtensions
{
    //public static void AddAndFillBlanks<T>(this List<T> list, T item, int position)
    //{
    //    for (int i = list.Count; i <= position; i++)
    //    {
    //        if (i >= list.Count)
    //            list.Add(default);
    //    }
    //    list[position] += item;
    //}
} 